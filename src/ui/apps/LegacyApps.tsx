import {useEffect,useState}from'react';import*as I from'lucide-react';import type{AppId,State}from'../../core/model';import{useGame}from'../../store';import{tr}from'../../data/content';import{date,money}from'../format';import{ContactAvatar}from'./chat/ContactAvatar';
const Card=({children}:{children:React.ReactNode})=><article className="card">{children}</article>;
function Avatar({n,id}:{n:string;id?:string}){const npc={id:id||n,name:n,age:0,role:'',personality:'',education:'',occupation:'',income:0,goal:'',lifeStage:'',closeness:'relevant' as const,relationship:{familiarity:0,warmth:0,trust:0,tension:0},memories:[]};return <ContactAvatar npc={npc}/>}
function School({g}:{g:State}){const act=useGame(x=>x.study),[duration,setDuration]=useState(.5);const exams=g.events.filter(e=>e.type==='exam'&&!e.processed);return <div className="content"><div className="headline"><div><p>10. SINIF</p><h1>{g.education.school}</h1></div><strong>Enerji %{g.player.energy}</strong></div><div className="pills">{([[.5,'30 dk'],[1,'1 saat'],[2,'2 saat']] as const).map(([v,n])=><button className={duration===v?'active':''} key={v} onClick={()=>setDuration(v)}>{n}</button>)}</div>{exams.map(e=><Card key={e.id}><small>YAKLAŞAN SINAV · {date(e.at)}</small><b>{e.title}</b></Card>)}<h3>Dersler ve çalışma</h3><div className="cards">{Object.entries(g.education.knowledge).map(([k,v])=><Card key={k}><div className="row"><b>{k}</b><span>%{v}</span></div><progress value={v} max="100"/><button onClick={()=>act(k,duration)}>{duration===.5?'30 dakika':duration+' saat'} çalış</button></Card>)}</div><h3>Sonuçlar</h3><Card>{g.education.mockScores.length?g.education.mockScores.map((x,i)=><span key={i}>Sınav {i+1}: {x} </span>):'Henüz sonuç yok.'}</Card></div>}
function Career({g}:{g:State}){const apply=useGame(x=>x.apply),clear=useGame(x=>x.clearCareerUnread);useEffect(()=>clear(),[clear]);return <div className="content"><div className="headline"><div><p>FIRSATLAR</p><h1>{g.player.province} için ilanlar</h1></div><strong>{g.applications.length} başvuru</strong></div><div className="cards">{g.jobs.map(j=>{const c=g.companies.find(x=>x.id===j.companyId)!,a=g.applications.find(x=>x.jobId===j.id);return <Card key={j.id}><small>{c.name} • {j.city}</small><h3>{j.position}</h3><p>{j.workType} · {j.education}<br/>{j.skills.join(', ')}</p><b>{money(j.salary)} / ay</b>{a?.state==='offer'?<><button onClick={()=>useGame.getState().offerDecision(a.id,true)}>Teklifi kabul et</button><button onClick={()=>useGame.getState().offerDecision(a.id,false)}>Reddet</button></>:<button disabled={!!a} onClick={()=>apply(j.id)}>{a?`Durum: ${a.state}`:'Başvur'}</button>}</Card>})}</div></div>}
function Stocks({g}:{g:State}){const trade=useGame(x=>x.trade),[selected,setSelected]=useState<string>();const c=g.companies.find(x=>x.id===selected);if(c){const h=g.holdings[c.id]||{quantity:0,cost:0},value=h.quantity*c.price,gain=value-h.cost,pts=c.priceHistory.map((v,i)=>`${(i/Math.max(1,c.priceHistory.length-1))*280},${70-(v/Math.max(...c.priceHistory))*60}`).join(' ');return <div className="content"><button className="back" onClick={()=>setSelected(undefined)}><I.ArrowLeft/> Şirketler</button><p className="fiction">Kurgusal şirket ve fiyatlar; finansal tavsiye değildir.</p><h1>{c.name}</h1><p>{c.sector} · Sağlık %{c.health} · İşe alım %{c.hiringDemand}</p><svg className="price-chart" viewBox="0 0 280 80" role="img" aria-label="Fiyat geçmişi"><polyline points={pts}/></svg><div className="stat-row"><Card><small>ADET</small><b>{h.quantity}</b></Card><Card><small>MALİYET</small><b>{money(h.cost)}</b></Card><Card><small>DEĞER</small><b>{money(value)}</b></Card><Card><small>FARK</small><b className={gain>=0?'gain':'loss'}>{money(gain)}</b></Card></div><button onClick={()=>trade(c.id,1)}>1 adet al</button>{h.quantity>0&&<button onClick={()=>trade(c.id,-1)}>1 adet sat</button>}</div>}return <div className="content"><p className="fiction">Tamamen kurgusal yerel piyasa; tavsiye değildir.</p><div className="cards">{g.companies.slice(0,16).map(c=><Card key={c.id}><small>{c.sector}</small><h3>{c.name}</h3><b>{money(c.price)}</b><p>{g.holdings[c.id]?.quantity||0} adet</p><button onClick={()=>setSelected(c.id)}>Şirketi aç</button></Card>)}</div></div>}
function Archive({g}:{g:State}){return <div className="content timeline"><h1>{g.player.name}’in Hayat Arşivi</h1>{g.archive.sort((a,b)=>b.at.localeCompare(a.at)).map((x,i)=><article key={i}><i/><div><small>{date(x.at)} • {x.category}</small><p>{x.text}</p></div></article>)}</div>}
function Calendar({g}:{g:State}){const s=useGame(),[selected,setSelected]=useState<string>();const e=g.events.find(x=>x.id===selected),app=e?.entityIds?.[0]&&g.applications.find(a=>a.id===e.entityIds?.[0]);if(e)return <div className="content"><button className="back" onClick={()=>setSelected(undefined)}><I.ArrowLeft/> Takvim</button><h1>{e.title}</h1><p>{date(e.at,{dateStyle:'full',timeStyle:'short'})}</p><Card><p>{e.details||'Takvimindeki planlı bir olay.'}</p><b>{e.processed?'Tamamlandı':e.requiresInput?'Kararın bekleniyor':'Planlandı'}</b>{e.type==='interview'&&!e.processed&&Date.parse(g.now)>=Date.parse(e.at)&&app&&<div className="interview-actions"><h3>Görüşmeye katıl</h3><button onClick={()=>s.attendInterview(app.id,'honest')}>Dürüst ve sakin konuş</button><button onClick={()=>s.attendInterview(app.id,'prepared')}>Hazırlığını örneklerle anlat</button><button onClick={()=>s.attendInterview(app.id,'confident')}>Kendinden emin davran</button><button onClick={()=>s.missInterview(app.id)}>Görüşmeye katılma</button></div>}</Card></div>;return <div className="content"><h1>Yaklaşanlar</h1>{g.events.sort((a,b)=>a.at.localeCompare(b.at)).map(e=><button className={`event ${e.processed?'done':''}`} key={e.id} onClick={()=>setSelected(e.id)}><time>{date(e.at,{day:'numeric',month:'short'})}</time><div><b>{e.title}</b><small>{e.processed?'Tamamlandı':e.requiresInput?'Karar bekliyor':e.important?'Önemli olay':'Planlandı'}</small></div></button>)}</div>}
function Notes({g}:{g:State}){const set=useGame(x=>x.setNotes);return <div className="content notes"><label htmlFor="notes">Kişisel notların</label><textarea id="notes" value={g.notes} onChange={e=>set(e.target.value)} placeholder="Çalışma planı, hedefler, hatırlatmalar…"/><small>Otomatik olarak yerel kayda eklenir.</small></div>}
function Settings({g}:{g:State}){const s=useGame();return <div className="content"><h1>Ayarlar</h1>{([['sound','Sesler'],['reducedMotion','Azaltılmış hareket'],['largeText','Büyük metin']]as const).map(([k,n])=><button className="setting" key={k} onClick={()=>s.setting(k,!g.settings[k])}><span>{n}</span><b>{g.settings[k]?'Açık':'Kapalı'}</b></button>)}<h3>Duvar kâğıdı</h3><div className="pills">{([['city','Şehir'],['coast','Sahil'],['simple','Sade']]as const).map(([v,n])=><button className={g.settings.wallpaper===v?'active':''} onClick={()=>s.wallpaper(v)} key={v}>{n}</button>)}</div><Card><I.ShieldCheck/><h3>Gizlilik</h3><p>{tr.privacy}</p></Card><Card><h3>Yerel kayıt</h3><p>İçe aktarılan dosya önce sürümlenir ve katı şemayla doğrulanır. Hatalı dosya mevcut hayatı değiştirmez.</p><button onClick={()=>void s.save()}>Şimdi kaydet</button><button onClick={()=>void s.exportSave()}>JSON dışa aktar</button><label className="import-button">JSON içe aktar<input type="file" accept="application/json" onChange={e=>{const f=e.target.files?.[0];if(f)void s.importSave(f)}}/></label></Card></div>}
function Feed({g}:{g:State}){const s=useGame();return <div className="content feed">{g.posts.map(p=>{const n=g.npcs.find(x=>x.id===p.npcId)!;return <Card key={p.id}><header><Avatar n={n.name}/><div><b>{n.name}</b><small>{date(p.at)}</small></div></header><p>{p.text}</p><button aria-label={p.liked?'Beğeniyi kaldır':'Beğen'} onClick={()=>s.like(p.id)}><I.Heart fill={p.liked?'currentColor':'none'}/> {p.likes}</button><details><summary>{p.comments.length} yorum</summary>{p.comments.map(c=><p key={c.id}><b>{c.authorId==='player'?'Sen':'Yakın çevre'}:</b> {c.text}</p>)}<form onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget),v=String(f.get('comment')||'');s.comment(p.id,v);e.currentTarget.reset()}}><input name="comment" aria-label="Yorum"/><button>Yaz</button></form></details></Card>})}</div>}
function Mail({g}:{g:State}){const s=useGame(),[open,setOpen]=useState<string>();const active=g.mails.find(m=>m.id===open);if(active)return <div className="content"><button className="back" onClick={()=>setOpen(undefined)}><I.ArrowLeft/> Gelen kutusu</button><h1>{active.subject}</h1><small>{active.sender} · {date(active.at)}</small><p>{active.body}</p>{active.app&&<button onClick={()=>s.open(active.app!)}>İlgili uygulamayı aç</button>}</div>;return <div className="content"><h1>Gelen kutusu</h1>{g.mails.map(m=><button className={`mail ${m.read?'read':''}`} key={m.id} onClick={()=>{s.readMail(m.id);setOpen(m.id)}}><I.Mail/><div><b>{m.sender}</b><strong>{m.subject}</strong><p>{m.body}</p></div><small>{date(m.at)}</small></button>)}</div>}
function News({g}:{g:State}){return <div className="content"><p className="fiction">Kurgusal haber · Gerçek kişi, şirket veya veri içermez.</p>{g.news.length?g.news.map(n=><Card key={n.id}><small>{n.category} · {date(n.at)}</small><h3>{n.title}</h3><p>{n.body}</p></Card>):<Card><h3>Dünya henüz sakin</h3><p>Zaman ilerledikçe şirket, okul ve şehir gelişmeleri burada oluşur.</p></Card>}</div>}
function MapApp({g}:{g:State}){const s=useGame(),[dest,setDest]=useState(''),[mode,setMode]=useState('Yürü'),job=g.employment&&g.jobs.find(j=>j.id===g.employment?.jobId),company=job&&g.companies.find(c=>c.id===job.companyId),interview=g.events.find(e=>e.type==='interview'&&!e.processed);const base=[['Ev',I.House],['Okul',I.GraduationCap],['Kafe',I.Coffee],['Market',I.ShoppingBasket],['Hastane',I.Hospital]],places:[string,typeof I.House][]=base as [string,typeof I.House][];if(company)places.push([company.name,I.BriefcaseBusiness]);if(interview)places.push(['Görüşme noktası',I.MapPin]);return <div className="content map"><p>Şu an: <b>{g.location}</b></p><div className="map-canvas">{places.map(([n,Icon],i)=><button style={{left:`${15+(i*17)%76}%`,top:`${18+(i*21)%68}%`}} onClick={()=>setDest(n)} key={n}><Icon/><span>{n}</span></button>)}</div><Card><h3>{dest||'Bir varış noktası seç'}</h3><div className="pills">{['Yürü','Otobüs','Minibüs','Taksi',...(g.vehicles.length?['Aracım']:[])].map(m=><button className={mode===m?'active':''} key={m} onClick={()=>setMode(m)}>{m}</button>)}</div><button disabled={!dest||dest===g.location} onClick={()=>s.travel(dest,mode)}>Yola çık</button></Card></div>}
export function LegacyApp({app,game}:{app:AppId;game:State}){if(app==='school')return <School g={game}/>;if(app==='career')return <Career g={game}/>;if(app==='stocks')return <Stocks g={game}/>;if(app==='archive')return <Archive g={game}/>;if(app==='calendar')return <Calendar g={game}/>;if(app==='notes')return <Notes g={game}/>;if(app==='settings')return <Settings g={game}/>;if(app==='feed')return <Feed g={game}/>;if(app==='mail')return <Mail g={game}/>;if(app==='news')return <News g={game}/>;if(app==='map')return <MapApp g={game}/>;return null}
