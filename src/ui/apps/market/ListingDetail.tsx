import {ArrowLeft,Heart,MessageCircle,SearchCheck} from 'lucide-react';import {useState} from 'react';import type {Listing} from '../../../core/model';import {useGame} from '../../../store';import {money} from '../../format';import {ListingThumbnail} from './ListingThumbnail';
export function ListingDetail({listing,onBack}:{listing:Listing;onBack:()=>void}){const s=useGame(),[offer,setOffer]=useState(''),n=listing.negotiation;return <main className="market-detail"><header><button onClick={onBack}><ArrowLeft/> İlanlara dön</button><button onClick={()=>s.toggleFavorite(listing.id)} aria-label={listing.favorite?'Favorilerden çıkar':'Favoriye ekle'}><Heart fill={listing.favorite?'currentColor':'none'}/></button></header><ListingThumbnail listing={listing} large/><section className="market-detail-copy"><small>{listing.category} · %{listing.condition} kondisyon</small><h1>{listing.title}</h1><strong className="market-price">{money(n?.agreedPrice??listing.price)}</strong><p>{listing.details}</p><dl><div><dt>Satıcı</dt><dd>{listing.seller}</dd></div><div><dt>Piyasa değeri</dt><dd>{money(listing.marketValue)}</dd></div><div><dt>İnceleme</dt><dd>{n?.inspected?(n.revealedIssue||'Belirgin sorun görülmedi'):'Yapılmadı'}</dd></div><div><dt>Görüşme</dt><dd>{n?.agreedPrice?'Fiyatta anlaşıldı':n?.counterOffer?`Karşı teklif: ${money(n.counterOffer)}`:n?.contacted?'Satıcıyla iletişimde':'Henüz başlanmadı'}</dd></div></dl><div className="market-secondary-actions"><button onClick={()=>s.contact(listing.id)}><MessageCircle/> Satıcıya sor</button><button onClick={()=>s.inspect(listing.id)}><SearchCheck/> İncele</button></div><form className="market-offer" onSubmit={e=>{e.preventDefault();const value=Number(offer.replace(',','.'));if(Number.isFinite(value)&&value>0)s.offer(listing.id,Math.round(value*100));else s.setError('Geçerli bir TL tutarı gir.')}}><label>Teklif (TL)<input inputMode="decimal" value={offer} onChange={e=>setOffer(e.target.value)} placeholder={(listing.price/100).toFixed(0)}/></label><button>Teklif gönder</button></form>{n?.counterOffer&&<button className="market-counter" onClick={()=>s.counter(listing.id)}>Karşı teklifi kabul et ({money(n.counterOffer)})</button>}<button className="market-buy" onClick={()=>s.buy(listing.id)}>{n?.agreedPrice?'Anlaşılan fiyata satın al':'İstenen fiyata satın al'}</button></section></main>}
