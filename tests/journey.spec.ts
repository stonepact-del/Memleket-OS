import{test,expect}from'@playwright/test';test('new life, action, time, notification and reload',async({page})=>{await page.goto('/');await page.getByRole('button',{name:'Yeni Hayat'}).click();await page.getByLabel('Karakter adı').fill('Deniz Kaya');await page.getByRole('button',{name:'Hayatı başlat'}).click();await expect(page.getByText('Merhaba, Deniz.')).toBeVisible();await page.getByRole('button',{name:/Okulum/}).first().click();await page.getByRole('button',{name:'2 saat çalış'}).first().click();await page.getByRole('button',{name:'+4 gün'}).click();if(await page.getByLabel('Kapat').isVisible())await page.getByLabel('Kapat').click();else await page.getByRole('button',{name:'M',exact:true}).click();await expect(page.getByText('Sınav sonucu')).toBeVisible();await page.reload();await expect(page.getByText('Merhaba, Deniz.')).toBeVisible();});
